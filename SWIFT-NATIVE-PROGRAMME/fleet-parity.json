{
 "activation": "NOT activated: no credentialed call has been made by this package; STOP-F6-AUTH live scope/readback/failure receipt is due at first control-plane use (F13/F14 wave). F7 fleet apply is gated on F14's own preimage locks.",
 "composition": {
  "gaps owned by GitHub.Control": [
   "AppCredential (mint composed via sanctioned `workspace github token`; no signing, no key exposure)",
   "RetryPolicy (secondary-rate-limit/5xx with idempotency guard; non-idempotent never retries; primary-limit exhaustion refuses)",
   "PageSequence (complete pagination; budget overflow is an error, never truncation)",
   "Client composition binding credential+retry+http"
  ],
  "swift-github": "witness-client domain layer (CO-03), head at resolution recorded in build log",
  "swift-github-http": "transport-free HTTP adapter with Link pagination witness (CO-03)"
 },
 "fleet": {
  "Fleet.Convergence": "Plan (preimage digest + reverse payload before any write), Apply (journal state machine; preimage drift refuses; illegal transitions refuse), Resume (resume-not-restart; shuffled journal refuses), Readback (post-state comparison)",
  "Fleet.Inventory.Census": "portable per-root rows; refuses uncontrolled zeros and duplicate repositories",
  "tests": "8/8 green via workspace package test --fresh (K-22 shapes: interruption resume, shuffle refusal, drift refusal)"
 },
 "goal": "swift-institute/.github#358",
 "package": "Tools/InstituteCIControl ('Institute CI Control')",
 "schemaVersion": 1,
 "stops": "STOP-F7-DURABILITY did not fire; STOP-F6-AUTH gate remains armed (no activation attempted)",
 "subIssues": {
  "F6": "swift-institute/.github#372",
  "F7": "swift-institute/.github#373"
 },
 "transaction": "F6+F7 (models)"
}