# Move-fast posture (principal ruling, 2026-08-05)

The ecosystem is pre-release: full speed, move fast and break things, no
backwards compatibility, avoid intermittent states; existing SemVer tags are
heritage and removable. Applied to this programme:

1. Parity windows collapse to single measured cutovers (one fixture-corpus
   comparison + one live candidate canary, then immediate authority
   transfer). No dual-authority period longer than one transaction.
2. Deletion joins activation: the incumbent is deleted in the same PR that
   activates its replacement, with the Class A preimage in that PR. Zero-use
   proof only where a consumer outside the transaction could exist.
3. No compatibility surfaces at all; F13/F14/F15 compress into the smallest
   coordinated waves the canaries allow, including the two-name secret
   cutover in the caller wave itself.
4. Not waived: evidence-first discipline (UNMEASURED over silence, run-level
   reads), the single required check staying green through every wave, no
   untrusted-credential exposure, exact-head canaries before central
   changes, principal-only destructive actions, and the FT1 ratification
   gate. Speed waives compatibility and staging, never safety or evidence.
