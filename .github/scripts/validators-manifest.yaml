# validators-manifest.yaml — single source-of-truth for rule-ID ↔
# validator-script ↔ workflow-file binding.
#
# Phase B-1 of CI-REVIEW-PHASE-B-DESIGN-2026-05-14 §3. The binding
# validator (validate-manifest-binding.py) cross-checks this manifest
# for internal consistency. Phase B-1 scope = checks 2 + 4 only
# (internal consistency); checks 1 + 3 (Skills cross-references)
# DEFERRED to Phase B-2 per supervisor adjudication 2026-05-14:
# auth-surface design for Skills checkout / Skills-scoped App-token
# mint OR alternative (sync-skills.sh-style [VERIFICATION:]-index
# mirror) made deliberately in B-2 rather than conflated into the
# B-1 canary.
#
# Schema:
#   - rule-id            (string) — the rule this entry binds.
#   - validator-script   (string) — path under swift-institute/.github
#                                   to the validator .py; empty when
#                                   status=deprecated.
#   - workflow-file      (string) — path under swift-institute/.github
#                                   to the validator workflow .yml;
#                                   empty when status=deprecated or
#                                   when the validator runs only via
#                                   another workflow (e.g., embedded
#                                   in a cron orchestrator).
#   - status             (enum)   — active | deferred | deprecated.
#   - self-firing        (enum)   — active | deferred | deprecated.
#   - discovery-mode     (string) — single-target |
#                                   wrapper-host-multi-file |
#                                   per-package-org-sweep |
#                                   hybrid (single-target self-test +
#                                   per-package-org-sweep weekly, both via
#                                   validate-base.yml; landed Task 0B-01,
#                                   swift-institute/.github#280) | (empty
#                                   for non-base-eligible).
#   - rule-id-regex      (string) — awk-compatible regex matching
#                                   column 2 of /tmp/findings.tsv.
#                                   Empty for deprecated entries.
#   - migrate-to-base    (string) — yes | no | <reason>. Annotated 'no
#                                   (chain-C; see CI-REVIEW-PHASE-B-DESIGN
#                                   §5)' for validate-thin-callers.yml
#                                   and validate-file-naming.yml per
#                                   the 4-level workflow_call limit.
#
# Writer / reader contract:
#   - Appended in the same Phase 7 transaction as a new validator
#     mechanization per [PROMOTE-006].
#   - Read by validate-manifest-binding.py weekly (Phase D's
#     lint-validators-weekly when authored) and on every
#     .github/scripts/validate-*.py edit (paths-trigger).
#
# Cross-references: CI-REVIEW-PHASE-B-DESIGN-2026-05-14.md §3 (schema)
#                  + supervisor adjudication 2026-05-14 (B-1 scope).

validators:
  # ─── ci-cd-workflows skill (active CI-* mechanized rules) ──────────

  - rule-id: CI-004b
    validator-script: .github/scripts/validate-sub-org-wrappers.py
    workflow-file: .github/workflows/validate-sub-org-wrappers.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^CI-004b'
    migrate-to-base: 'no (per-package-org-sweep deferred to B-2)'

  - rule-id: CI-010
    validator-script: .github/scripts/validate-ci-matrix.py
    workflow-file: .github/workflows/validate-ci-matrix.yml
    status: active
    self-firing: active
    discovery-mode: single-target
    rule-id-regex: '^CI-010'
    migrate-to-base: yes

  - rule-id: CI-021
    validator-script: .github/scripts/validate-embedded-job.py
    workflow-file: .github/workflows/validate-embedded-job.yml
    status: active
    self-firing: active
    discovery-mode: single-target
    rule-id-regex: '^CI-021'
    migrate-to-base: yes

  - rule-id: CI-030
    validator-script: .github/scripts/validate-thin-callers.py
    workflow-file: .github/workflows/validate-thin-callers.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^CI-030'
    migrate-to-base: 'no (chain-C; see CI-REVIEW-PHASE-B-DESIGN §5)'

  - rule-id: CI-032
    validator-script: .github/scripts/validate-visibility-gate.py
    workflow-file: .github/workflows/validate-visibility-gate.yml
    status: active
    self-firing: active
    discovery-mode: wrapper-host-multi-file
    rule-id-regex: '^CI-032'
    migrate-to-base: yes

  - rule-id: CI-040
    validator-script: .github/scripts/validate-cache-policy.py
    workflow-file: .github/workflows/validate-cache-policy.yml
    status: active
    self-firing: active
    discovery-mode: wrapper-host-multi-file
    rule-id-regex: '^CI-040'
    migrate-to-base: yes

  - rule-id: CI-042
    validator-script: .github/scripts/validate-cache-policy.py
    workflow-file: .github/workflows/validate-cache-policy.yml
    status: active
    self-firing: active
    discovery-mode: wrapper-host-multi-file
    rule-id-regex: '^CI-042'
    migrate-to-base: yes

  - rule-id: CI-055
    validator-script: ''
    workflow-file: ''
    status: deprecated
    self-firing: deprecated
    discovery-mode: ''
    rule-id-regex: ''
    migrate-to-base: 'n/a (deprecated 2026-05-14; migrated to SwiftLint Tier 2 custom rule)'

  - rule-id: CI-058
    validator-script: .github/scripts/validate-input-defaults.py
    workflow-file: .github/workflows/validate-input-defaults.yml
    status: active
    self-firing: active
    discovery-mode: wrapper-host-multi-file
    rule-id-regex: '^CI-058'
    migrate-to-base: yes

  - rule-id: CI-059
    validator-script: .github/scripts/validate-thin-callers.py
    workflow-file: .github/workflows/validate-thin-callers.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^CI-059'
    migrate-to-base: 'no (chain-C; see CI-REVIEW-PHASE-B-DESIGN §5)'

  - rule-id: CI-080
    validator-script: .github/scripts/validate-harden-runner.py
    workflow-file: .github/workflows/validate-harden-runner.yml
    status: active
    self-firing: active
    discovery-mode: wrapper-host-multi-file
    rule-id-regex: '^CI-080'
    migrate-to-base: yes

  - rule-id: CI-082
    validator-script: .github/scripts/validate-binary-install-checksum.py
    workflow-file: .github/workflows/validate-binary-install-checksum.yml
    status: active
    self-firing: active
    discovery-mode: wrapper-host-multi-file
    rule-id-regex: '^CI-082'
    migrate-to-base: yes

  - rule-id: CI-090
    validator-script: .github/scripts/validate-permissions-shape.py
    workflow-file: .github/workflows/validate-permissions-shape.yml
    status: active
    self-firing: active
    discovery-mode: wrapper-host-multi-file
    rule-id-regex: '^CI-090'
    migrate-to-base: yes

  - rule-id: CI-097
    validator-script: .github/scripts/validate-permissions-shape.py
    workflow-file: .github/workflows/validate-permissions-shape.yml
    status: active
    self-firing: active
    discovery-mode: wrapper-host-multi-file
    rule-id-regex: '^CI-097'
    migrate-to-base: yes

  - rule-id: CI-099
    validator-script: .github/scripts/validate-ci-matrix.py
    workflow-file: .github/workflows/validate-ci-matrix.yml
    status: active
    self-firing: active
    discovery-mode: single-target
    rule-id-regex: '^CI-099'
    migrate-to-base: yes

  - rule-id: CI-100
    validator-script: .github/scripts/validate-swiftlint-rules.py
    workflow-file: .github/workflows/validate-swiftlint-rules.yml
    status: active
    self-firing: active
    discovery-mode: single-target
    rule-id-regex: '^CI-100'
    migrate-to-base: yes

  - rule-id: CI-102
    validator-script: .github/scripts/validate-composite-action-descriptions.py
    workflow-file: .github/workflows/validate-composite-action-descriptions.yml
    status: active
    self-firing: active
    discovery-mode: wrapper-host-multi-file
    rule-id-regex: '^CI-102'
    migrate-to-base: yes

  - rule-id: CI-103
    validator-script: .github/scripts/validate-env-context.py
    workflow-file: .github/workflows/validate-env-context.yml
    status: active
    self-firing: active
    discovery-mode: wrapper-host-multi-file
    rule-id-regex: '^CI-103'
    migrate-to-base: yes

  - rule-id: CI-105
    validator-script: .github/scripts/validate-continue-on-error.py
    workflow-file: .github/workflows/validate-continue-on-error.yml
    status: active
    self-firing: active
    discovery-mode: wrapper-host-multi-file
    rule-id-regex: '^CI-105'
    migrate-to-base: yes

  - rule-id: CI-117
    validator-script: .github/scripts/validate-composite-action-pins.py
    workflow-file: ''
    status: active
    self-firing: active
    discovery-mode: single-target
    rule-id-regex: '^CI-117'
    migrate-to-base: 'no (repo-local self-check of swift-institute/.github''s own workflow files — corrigendum §11.1, swift-institute/.github#286 — not a fleet per-repo scan; validate-base.yml''s per-package-org-sweep shape does not apply)'

  - rule-id: CI-MANIFEST-BINDING
    validator-script: .github/scripts/validate-manifest-binding.py
    workflow-file: .github/workflows/validate-manifest-binding.yml
    status: active
    self-firing: active
    discovery-mode: single-target
    rule-id-regex: '^CI-MANIFEST-BINDING'
    migrate-to-base: yes

  # ─── Cross-cutting (gh-repo skill, shared with thin-callers) ───────

  - rule-id: GH-REPO-074
    validator-script: .github/scripts/validate-thin-callers.py
    workflow-file: .github/workflows/validate-thin-callers.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^GH-REPO-074'
    migrate-to-base: 'no (chain-C; see CI-REVIEW-PHASE-B-DESIGN §5)'

  # ─── Non-ci-cd-workflows validators (one representative entry per
  #     script to satisfy check 2; multi-rule expansion + skill
  #     cross-checks deferred to B-2) ──────────────────────────────

  - rule-id: API-IMPL-006
    validator-script: .github/scripts/validate-file-naming.py
    workflow-file: .github/workflows/validate-file-naming.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^API-IMPL-006'
    migrate-to-base: 'no (chain-C; see CI-REVIEW-PHASE-B-DESIGN §5)'

  - rule-id: API-IMPL-007
    validator-script: .github/scripts/validate-file-naming.py
    workflow-file: .github/workflows/validate-file-naming.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^API-IMPL-007'
    migrate-to-base: 'no (chain-C; see CI-REVIEW-PHASE-B-DESIGN §5)'

  - rule-id: API-NAME-009
    validator-script: .github/scripts/validate-diagnostic-format.py
    workflow-file: ''
    status: active
    self-firing: active
    discovery-mode: single-target
    rule-id-regex: '^API-NAME-009'
    migrate-to-base: yes

  - rule-id: BRANCH-PIN-001
    validator-script: .github/scripts/validate-branch-pins.py
    workflow-file: .github/workflows/validate-branch-pins.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^BRANCH-PIN-001'
    migrate-to-base: 'no (per-package-org-sweep deferred to B-2)'

  - rule-id: DOC-CATALOG
    validator-script: .github/scripts/validate-docc-structure.py
    workflow-file: ''
    status: active
    self-firing: active
    discovery-mode: single-target
    rule-id-regex: '^DOC-'
    migrate-to-base: yes

  - rule-id: GH-IGNORE-001
    validator-script: .github/scripts/validate-gitignore.py
    workflow-file: .github/workflows/validate-gitignore.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^GH-IGNORE-001'
    migrate-to-base: 'no (per-package-org-sweep deferred to B-2)'

  - rule-id: GH-IGNORE-002
    validator-script: .github/scripts/validate-gitignore.py
    workflow-file: .github/workflows/validate-gitignore.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^GH-IGNORE-002'
    migrate-to-base: 'no (per-package-org-sweep deferred to B-2)'

  - rule-id: GH-REPO-METADATA
    validator-script: .github/scripts/validate-github-metadata.py
    workflow-file: .github/workflows/validate-github-metadata.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^GH-REPO-'
    migrate-to-base: 'no (signature mismatch + jsonschema cross-repo fetch; rewrite scope > migration)'

  - rule-id: PACKAGE-STRUCTURE
    validator-script: .github/scripts/validate-package-structure.py
    workflow-file: .github/workflows/validate-package-structure.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^PACKAGE-'
    migrate-to-base: 'no (swift-package-describe shell logic + macos runner; out of B-2 envelope)'

  - rule-id: PATTERN-001
    validator-script: .github/scripts/validate-package-shape.py
    workflow-file: ''
    status: active
    self-firing: active
    discovery-mode: single-target
    rule-id-regex: '^PATTERN-'
    migrate-to-base: yes

  - rule-id: PLAT-ARCH-004
    validator-script: .github/scripts/validate-platform-architecture.py
    workflow-file: .github/workflows/validate-platform-architecture.yml
    status: active
    self-firing: active
    discovery-mode: 'hybrid (single-target self-test + per-package-org-sweep weekly; Task 0B-01, swift-institute/.github#280)'
    rule-id-regex: '^PLAT-ARCH-'
    migrate-to-base: yes

  - rule-id: PLAT-ARCH-008
    validator-script: .github/scripts/validate-layer-deps.py
    workflow-file: ''
    status: active
    self-firing: active
    discovery-mode: 'hybrid (single-target self-test + per-package-org-sweep weekly; Task 0B-01, swift-institute/.github#280)'
    rule-id-regex: '^PLAT-ARCH-008'
    migrate-to-base: yes

  - rule-id: README-PRESENCE
    validator-script: .github/scripts/validate-readme.py
    workflow-file: ''
    status: active
    self-firing: active
    discovery-mode: 'hybrid (single-target self-test + per-package-org-sweep weekly; Task 0B-01, swift-institute/.github#280)'
    rule-id-regex: '^README-'
    migrate-to-base: yes

  - rule-id: PKG-DEP-008
    validator-script: .github/scripts/validate-package-identity.py
    workflow-file: .github/workflows/validate-package-identity.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^PKG-DEP-008'
    migrate-to-base: 'no (per-package-org-sweep deferred to B-2)'

  - rule-id: PKG-DEP-009
    validator-script: .github/scripts/validate-dependency-spelling.py
    workflow-file: .github/workflows/validate-dependency-spelling.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^PKG-DEP-009'
    migrate-to-base: 'no (per-package-org-sweep deferred to B-2)'

  - rule-id: ARCH-LAYER-012
    validator-script: .github/scripts/validate-test-target-layers.py
    workflow-file: .github/workflows/validate-test-target-layers.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^ARCH-LAYER-012'
    migrate-to-base: 'no (per-package-org-sweep deferred to B-2)'

  - rule-id: GH-REPO-063
    validator-script: .github/scripts/validate-schema-workflow-keys.py
    workflow-file: .github/workflows/validate-schema-correspondence.yml
    status: active
    self-firing: 'yes'
    discovery-mode: single-target
    rule-id-regex: '^GH-REPO-063'
    migrate-to-base: 'no (takes three file paths and emits prose, not the <repo> <repo-root> TSV convention validate-base.yml and tests/run.sh assume)'
    # Was `self-firing: deferred` until 2026-07-28. The guard ran only inside
    # validate-github-metadata.yml, which has no push/pull_request trigger and
    # checks out `ref: main`, so a pull request introducing the exact drift it
    # detects merged green. validate-schema-correspondence.yml now runs it on
    # push and pull_request against the ref under review, gated by a fixture
    # suite under tests/schema-correspondence/ whose fail/ scenarios were each
    # watched to fire. The embedded call in validate-github-metadata.yml stays:
    # that is the sweep's view of main, this is the gate on the change.
    #
    # Scope also widened the same day, from schema<->workflow to
    # schema<->consumer: the guard now covers metadata-schema.json's `readme`
    # block against the EXEMPTIONS and FAMILIES constants in validate-readme.py,
    # a correspondence that previously had no machine check of any kind.

  - rule-id: PRIM-NAME-001
    validator-script: .github/scripts/validate-package-naming.py
    workflow-file: .github/workflows/validate-package-naming.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^PRIM-NAME-001'
    migrate-to-base: 'no (per-package-org-sweep deferred to B-2)'

  - rule-id: PACKAGE-CYCLE
    validator-script: .github/scripts/validate-package-graph.py
    workflow-file: ''
    status: active
    self-firing: deferred
    discovery-mode: ''
    rule-id-regex: '^PACKAGE-CYCLE'
    migrate-to-base: 'no (whole-graph multi-root scan; CI form needs multi-repo checkout design — deferred with B-2 / dep-graph tooling)'

  - rule-id: PRIM-ARCH-002
    validator-script: .github/scripts/validate-package-graph.py
    workflow-file: ''
    status: active
    self-firing: deferred
    discovery-mode: ''
    rule-id-regex: '^PACKAGE-CYCLE'
    migrate-to-base: 'no (covered by PACKAGE-CYCLE acyclicity — computed tiers exist iff acyclic; findings in the primitives org cite both IDs)'

  - rule-id: PKG-NAME-014
    validator-script: .github/scripts/validate-package-naming.py
    workflow-file: .github/workflows/validate-package-naming.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^PKG-NAME-014'
    migrate-to-base: 'no (per-package-org-sweep deferred to B-2)'

  - rule-id: PKG-NAME-EXTERNAL-MACRO
    validator-script: .github/scripts/validate-package-naming.py
    workflow-file: .github/workflows/validate-package-naming.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^PKG-NAME-EXTERNAL-MACRO'
    migrate-to-base: 'no (per-package-org-sweep deferred to B-2)'

  - rule-id: PKG-NAME-017
    validator-script: .github/scripts/validate-package-naming.py
    workflow-file: .github/workflows/validate-package-naming.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^PKG-NAME-017'
    migrate-to-base: 'no (per-package-org-sweep deferred to B-2)'

  - rule-id: TARGET-IMPORT-EDGE
    validator-script: .github/scripts/validate-target-imports.py
    workflow-file: ''
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^TARGET-IMPORT-EDGE'
    migrate-to-base: 'no (needs dump-package + local dep-manifest resolution; CI form joins the B-2 sweep design)'

  # swift-institute/.github#219 (principal ruling, comment 5164746902,
  # 2026-08-03): synthetic-fixture-only self-check against this repo's own
  # canonical .swiftlint.yml (never an arbitrary target repo), so it has no
  # dedicated scan workflow — it is exercised entirely through
  # lint-validator-fixtures.yml's fixture suite. self-firing: active because
  # that workflow carries push + pull_request triggers over
  # .github/scripts/validate-*.py (this script included).
  - rule-id: SWIFTLINT-WITNESS-EXEMPTION
    validator-script: .github/scripts/validate-swiftlint-witness-exemption.py
    workflow-file: .github/workflows/lint-validator-fixtures.yml
    status: active
    self-firing: active
    discovery-mode: single-target
    rule-id-regex: '^SWIFTLINT-WITNESS-EXEMPTION'
    migrate-to-base: 'n/a (self-test only against this repo''s own canonical config; no scan job of its own)'

  # swift-institute/.github#242: same synthetic-fixture-only self-check
  # posture as SWIFTLINT-WITNESS-EXEMPTION above — verifies the
  # no_int_bitpattern_arithmetic newline-into-comment fix against this
  # repo's own canonical .swiftlint.yml, exercised entirely through
  # lint-validator-fixtures.yml (push + pull_request triggers over
  # .github/scripts/validate-*.py include this script).
  - rule-id: SWIFTLINT-BITPATTERN-COMMENT
    validator-script: .github/scripts/validate-swiftlint-bitpattern-comment.py
    workflow-file: .github/workflows/lint-validator-fixtures.yml
    status: active
    self-firing: active
    discovery-mode: single-target
    rule-id-regex: '^SWIFTLINT-BITPATTERN-COMMENT'
    migrate-to-base: 'n/a (self-test only against this repo''s own canonical config; no scan job of its own)'

  # ─── Skill corpus publication hygiene ──────────────────────────────
  #
  # One aggregate entry per the "representative entry per script"
  # convention above. The validator's findings are named for the
  # behaviour they describe (skill-frontmatter, skill-identity,
  # skill-links, skill-machine-path, skill-internal-rule-id,
  # skill-corpus-empty) rather than carrying requirement identifiers, so
  # the corpus it guards never has to cite one back. Check 3 does not
  # apply: it fires only for the ci-cd-workflows `^CI-\d+[a-z]?$` shape.
  #
  # self-firing: active — the workflow carries push + pull_request
  # triggers scoped to the validator, its fixtures and its own file. They
  # run the fixture self-test, which proves each check can still fail;
  # the scan job stays skipped here because this repo publishes no
  # skills.

  - rule-id: SKILL-HYGIENE
    validator-script: .github/scripts/validate-skill-hygiene.py
    workflow-file: .github/workflows/validate-skill-hygiene.yml
    status: active
    self-firing: active
    discovery-mode: single-target
    rule-id-regex: '^skill-'
    migrate-to-base: 'yes (single-target; landed on validate-base.yml)'

  # ─── Task 4-01 (swift-institute/.github#276, #284) ──────────────────
  # Not a numbered [CI-NNN] rule — no /promote-rule pilot slot claimed by
  # this task. INTEGRATED-DOCS-ADMISSION composes into validate-thin-
  # callers.py per the shared per-package-caller domain (same pattern as
  # CI-030/CI-059 above). The MARKER-MISSING/MARKER-ORPHANED repo-local
  # self-check retired with the integrated-docs input at TX10.

  - rule-id: INTEGRATED-DOCS-ADMISSION
    validator-script: .github/scripts/validate-thin-callers.py
    workflow-file: .github/workflows/validate-thin-callers.yml
    status: active
    self-firing: deferred
    discovery-mode: per-package-org-sweep
    rule-id-regex: '^INTEGRATED-DOCS-ADMISSION'
    migrate-to-base: 'no (chain-C; see CI-REVIEW-PHASE-B-DESIGN §5)'
