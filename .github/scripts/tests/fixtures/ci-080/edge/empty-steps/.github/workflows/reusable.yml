name: Edge — job has no steps and is not pure uses-only

# Unusual shape: job with runs-on but no steps. Validator skips (no
# first step to inspect). Expected: 0 findings.

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps: []
