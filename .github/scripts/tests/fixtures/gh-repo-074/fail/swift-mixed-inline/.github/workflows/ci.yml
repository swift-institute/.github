name: CI

on:
  pull_request:

jobs:
  central:
    uses: swift-foundations/.github/.github/workflows/swift-ci.yml@main
    secrets: inherit
  local:
    runs-on: ubuntu-latest
    steps:
      - run: swift test
