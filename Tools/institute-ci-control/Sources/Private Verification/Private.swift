// Nest.Name namespace shells (FT1-ratification.json).
// `Private.Verification` owns the split-credential private-verification
// relation: request, lease, exact-head verification, redaction, and safe
// publication. No credential ever enters candidate execution.
public enum Private {}

extension Private {
    public enum Verification {}
}
