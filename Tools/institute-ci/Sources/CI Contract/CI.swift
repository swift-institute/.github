// Nest.Name namespace shells (FT1-ratification.json;
// naming-annex-nest-name.md). `CI.Contract` owns subject/event/plan/
// aggregate semantics and preserves the required check context
// `ci / matrix / ci-ok`.
public enum CI {}

extension CI {
    public enum Contract {}
}
