// Nest.Name namespace shells (FT1-ratification.json;
// naming-annex-nest-name.md). `Institute.Receipt` owns canonical
// evidence, same-run identities, and completion/runtime readiness.
public enum Institute {}

extension Institute {
    public enum Receipt {}
}
