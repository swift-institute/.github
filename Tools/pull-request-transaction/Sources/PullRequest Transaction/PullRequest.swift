/// The pull-request domain namespace (Nest.Name; naming annex
/// `naming-annex-nest-name.md`, which controls this spelling). `PR` is an
/// abbreviation, not a noun, and `PRTransaction` was a flat compound; both
/// are retired here.
public enum PullRequest {}
