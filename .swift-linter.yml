# Tier 1 — ecosystem-wide canonical configuration for swift-linter
# (the SwiftSyntax-based AST linter at swift-foundations/swift-linter).
#
# Initially empty in Phase 1: rules graduate to Tier 1 only when they apply
# ecosystem-wide. R5 (`unchecked_call_site`) is primitives-specific and lives
# at Tier 2 (swift-primitives/.github/.swift-linter.yml).
#
# Schema: a flat `rules:` block listing activated rule IDs. `parent_config:`
# URL chaining mirroring SwiftLint's mechanism is deferred to Phase 2.
#
# Renamed 2026-05-06 (Phase 1.5 Item 2) from .swift-primitives-lint.yml so
# the canonical filename tracks the binary name `swift-linter` and the L3
# package `swift-linter`.

rules: []
